package ocp.collections;

public class CollectionsTest {
	/**
	 * Arrays.sort require an
	 * objects which implements Comparable. If the object,
	 * does not implement Comparable, it will throw a 
	 * runtime exception, it will not generate a compilation
	 * error.
	 * 
	 * 
	 * Map interface does NOT extend Collection interface.
	 */
	
}
